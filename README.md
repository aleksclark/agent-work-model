# Agent Work Model

A protocol-neutral semantic interoperability model for agent work.

Agent Work Model describes shared meaning across **Projects**, **WorkSessions**, resources, agents, and **AgentRuns**. It is intended as a common vocabulary that different agent systems, tools, and protocols can map to without adopting a particular transport or runtime.

This repository will hold the model and supporting artifacts. Concrete schemas and implementations are intentionally deferred.
